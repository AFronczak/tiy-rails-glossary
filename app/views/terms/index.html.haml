%h1 Terms
%table.table.table-striped.table-hover
  %thead
    %tr
      %th Name
      %th Definition
      %th Author
      %th Category
      %th Web link
      %th{:colspan => "3"}
  %tbody
    - @terms.each do |term|
      %tr
        %td= term.name
        %td= term.definition
        %td= term.author
        %td= term.category_id
        %td= term.web_link
        %td= link_to 'Show', term
        %td= link_to 'Edit', edit_term_path(term)
        %td= link_to 'Destroy', term_path(term),
                                method: :delete,
                                data: { confirm: 'Are you sure?'}
%br/
= link_to 'New Term', new_term_path
\|
=link_to 'Home', root_path
